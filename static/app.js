document.addEventListener('click',e=>{const t=e.target.closest('[data-confirm]');if(t&&!confirm(t.dataset.confirm))e.preventDefault()});
const shell=document.querySelector('.study-shell[data-cards]');
if(shell){const cards=JSON.parse(shell.dataset.cards||'[]');if(cards.length){let index=0,revealed=false;const card=document.querySelector('#flashcard'),front=document.querySelector('#front'),back=document.querySelector('#back'),answer=document.querySelector('#answer'),counter=document.querySelector('#counter'),bar=document.querySelector('#bar'),reveal=document.querySelector('#reveal'),next=document.querySelector('#next'),complete=document.querySelector('#complete'),actions=document.querySelector('.study-action');
function paint(){front.textContent=cards[index].front;back.textContent=cards[index].back;counter.textContent=`Card ${index+1} of ${cards.length}`;bar.style.width=`${(index/cards.length)*100}%`;revealed=false;card.classList.remove('revealed');answer.setAttribute('aria-hidden','true');reveal.classList.remove('hidden');next.classList.add('hidden');card.setAttribute('aria-label','Reveal answer')}
function show(){if(revealed)return;revealed=true;card.classList.add('revealed');answer.setAttribute('aria-hidden','false');reveal.classList.add('hidden');next.classList.remove('hidden');card.setAttribute('aria-label','Go to next card')}
function advance(){if(!revealed){show();return}if(index<cards.length-1){index++;card.animate([{opacity:.4,transform:'translateX(8px)'},{opacity:1,transform:'none'}],{duration:220});paint()}else{card.classList.add('hidden');actions.classList.add('hidden');complete.classList.remove('hidden');counter.textContent=`${cards.length} of ${cards.length}`;bar.style.width='100%'}}
card.addEventListener('click',advance);reveal.addEventListener('click',show);next.addEventListener('click',advance);document.querySelector('#again').addEventListener('click',()=>{index=0;card.classList.remove('hidden');actions.classList.remove('hidden');complete.classList.add('hidden');paint()});document.addEventListener('keydown',e=>{if(e.key===' '||e.key==='Enter'){if(e.target.matches('input,textarea,button'))return;e.preventDefault();show()}if(e.key==='ArrowRight'&&revealed)advance()});paint();}}
